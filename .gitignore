# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Debug or running production
cassemdb.pids

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
go.sum

# Folders
.idea/
debugdata/

# Binaries
cassemdb
cassemagent
cassemadm

# Excludes
!cassemdb/
!cassemagent/
!cassemadm/
!debugdata/cassemdb1.toml
!debugdata/cassemdb2.toml
!debugdata/cassemdb3.toml